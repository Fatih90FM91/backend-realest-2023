
{
    "builds": [{
        "src": "realest_estate/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.7" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "realest_estate/wsgi.py"
        }
    ]
}
